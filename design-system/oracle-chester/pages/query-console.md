# Page override · Query console (Oracle)

Overrides `MASTER.md` for `apps/web/public/index.html`.

MASTER suggested BI-blue + amber + Fira Code. Share the **Chester Field Desk** family with the CRM so the two hosted runtimes read as one product, not two templates.

## Job

Operator desk for the Chester harvest: coverage, source counts, radius query, agent evidence, MCP catalog. Tables first. No marketing hero.

## Tokens

Use the same remap as `roofing-crm/design-system/chester-field-desk/pages/lead-map.md`:

slate tray header, mylar page, copper CTA, plat-blue links, stamp-red for open permits, IBM Plex + Barlow Condensed, 2–4px radius, 8px density.

JSON/pre blocks: `--tray` background, `--tray-ink` type. Not matrix green.

## Do not break

Headings must still match e2e: `Oracle pipeline`, `Pipeline run summary`, `Agent query`, `MCP-ready`, `DuckDB-backed query layer`. IDs: `#run`, `#coverage`, `#query`, `#table`, `#ask`, `#agent`, `#q`.
